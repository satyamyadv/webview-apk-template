# ============================================================
#  WebView APK Template — App Configuration
#  Edit the values below, then build.
# ============================================================

# URL to load in the WebView on launch
https://solo-systems-9468.ai.studio/

# Application ID — unique identifier for your app
# (used by the Play Store and Android device; must be globally unique)
app.package=com.example.myapp

# App display name shown on the home screen
app.name=Solo-system

# Optional app icon URL or local path (relative paths start at the project root).
# Common raster formats, SVG, ICO, WebP, AVIF, and Android Vector Drawable are supported.
# Leave empty to use Android's system default icon. GitHub Actions tries /favicon.ico first.
app.icon_url=
